<resources>

    <style name="Theme.EcoPlant" parent="Theme.MaterialComponents.DayNight.NoActionBar">

        <item name="colorPrimary">@color/green_button</item> <!-- Couleur principale -->
        <item name="colorPrimaryVariant">@color/brown_button</item> <!-- Variante plus foncée -->
        <item name="colorOnPrimary">@color/text_green_button</item> <!-- Couleur du texte sur fond primaire -->
        <item name="colorSecondary">@color/yellow_button</item> <!-- Couleur secondaire -->
        <item name="colorOnSecondary">@color/text_yellow_button</item>

        <!-- Ne pas toucher sinon barre de navigation marche pas. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <item name="android:fontFamily">@font/poppins</item>
        <item name="fontFamily">@font/poppins</item>

        <item name="materialButtonStyle">@style/CustomMaterialButtonStyle</item>


    </style>

    <style name="CustomMaterialButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">339dp</item>
        <item name="android:layout_height">60dp</item>
    </style>





</resources>
